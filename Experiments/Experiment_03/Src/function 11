var options = [];

if (msg.payload && msg.payload.length > 0) {
    msg.payload.forEach(function(row) {
        if (row.device_id) {
            options.push({
                label: row.device_id,
                value: row.device_id
            });
        }
    });
}
msg.options = options;
return msg;